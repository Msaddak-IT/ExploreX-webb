<form method="post" action="{{ path('app_bonplan_delete', {'id': bonplan.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ bonplan.id) }}">
    <button class="btn">Delete</button>
</form>
