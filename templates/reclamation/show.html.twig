{% extends '/template/baseback.html.twig' %}

{% block title %}Reclamation{% endblock %}


{% block main %}
    <h1>Reclamation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reclamation.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ reclamation.type }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ reclamation.nom }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ reclamation.description }}</td>
            </tr>
            <tr>
                <th>Datet_reclama</th>
                <td>{{ reclamation.datetReclama ? reclamation.datetReclama|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_reclamation_index') }}">Retourner à la liste </a>

    <a href="{{ path('app_reclamation_edit', {'id': reclamation.id}) }}">modifier</a>

    {{ include('reclamation/_delete_form.html.twig') }}
{% endblock %}
