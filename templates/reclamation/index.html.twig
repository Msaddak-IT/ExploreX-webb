{% extends 'baseback.html.twig' %}

{% block title %}Reclamation index{% endblock %}

{% block main %}
    <h1>Reclamation index</h1>

         <div class="body-container">
    <div class="form-container">
     <form method="get" action="{{ path('app_reclamation_index') }}" style="margin-bottom: 20px;">
    <div class="form-row">
        <div class="col">
            <input type="text" name="search" class="form-control" placeholder="Search"  id="myInput">
        </div>
    </div>
    <button type="submit" class="btn btn-primary">Search</button>
    </form>
    </div>
    </div>

    

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Type</th>
                <th>Nom</th>
                <th>Description</th>
                <th>Datet_reclama</th>
                <th>Pdf</th>
                <th>Actions</th>

            </tr>
        </thead>
        <tbody id="myTable">
        {% for reclamation in reclamations %}
            {% if app.request.query.get('search') is empty or
          app.request.query.get('search') in [reclamation.Id] or
          app.request.query.get('search') in reclamation.Nom or
          app.request.query.get('search') in reclamation.Description %}
            

            <tr>
                <td>{{ reclamation.id }}</td>
                <td>{{ reclamation.type }}</td>
                <td>{{ reclamation.nom }}</td>
                <td>{{ reclamation.description }}</td>
                <td>{{ reclamation.datetReclama ? reclamation.datetReclama|date('Y-m-d H:i:s') : '' }}</td>
                   <td>
 <a href="{{ path('create_pdf', {'id': reclamation.id}) }}" class="btn btn-success" style="display: inline-block; padding: 10px 20px; font-size: 14px; font-weight: bold; text-align: center; text-decoration: none; background-color: #4CAF50; color: #fff; border: none; border-radius: 4px; cursor: pointer; transition: background-color 0.3s ease;">Avoir PDF</a>
            </td>
                <td>
                    <a href="{{ path('app_reclamation_show', {'id': reclamation.id}) }}">show</a>
                    <a href="{{ path('app_reclamation_edit', {'id': reclamation.id}) }}">edit</a>
                </td>
            </tr>
            {%endif %}
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
 
    <a href="{{ path('app_reclamation_new') }}">Create new</a>

    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
<script>
  $(document).ready(function(){

    $("#myInput").on("keyup", function() {
      var value = $(this).val().toLowerCase();

      $("#myTable tr").filter(function() {
        $(this).toggle($(this).text().toLowerCase().indexOf(value) > -1);
      });
    });

  });
</script>

{% endblock %}
