{% extends '/template/baseback.html.twig' %}


{% block title %}Remboursement{% endblock %}

{% block main %}
    <h1>Remboursement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ remboursement.id }}</td>
            </tr>
            <tr>
                <th>Montant_rembour</th>
                <td>{{ remboursement.montantRembour }}</td>
            </tr>
            <tr>
                <th>Date_rembour</th>
                <td>{{ remboursement.dateRembour ? remboursement.dateRembour|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Id_rec</th>
                <td>{{ remboursement.idRec }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_remboursement_index') }}">Retourner à la liste</a>

    <a href="{{ path('app_remboursement_edit', {'id': remboursement.id}) }}">modifier</a>

    {{ include('remboursement/_delete_form.html.twig') }}
{% endblock %}
