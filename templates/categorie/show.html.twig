{% extends 'backbase.html.twig' %}

{% block title %}Categorie{% endblock %}

{% block body1 %}
    <h1 style="color: #3498db;">Categorie</h1>

    <table class="table" style="border: 2px solid #3498db;">
        <tbody>
            <tr>
                <th style="background-color: #3498db; color: #fff;">Id</th>
                <td>{{ categorie.id }}</td>
            </tr>
            <tr>
                <th style="background-color: #3498db; color: #fff;">NomCategorie</th>
                <td>{{ categorie.nomCategorie }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_categorie_index') }}" class="btn btn-info mt-3" style="background-color: #3498db; color: #fff;">Back to List</a>

    <a href="{{ path('app_categorie_edit', {'id': categorie.id}) }}" class="btn btn-warning mt-3" style="background-color: #f39c12; color: #fff;">Edit</a>

    {{ include('categorie/_delete_form.html.twig') }}
{% endblock %}
